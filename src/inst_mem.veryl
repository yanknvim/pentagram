import utils::*;

module InstMem (
    addr: input  logic<32>,
    data: output logic<32>,
) {
    var mem: logic<32> [2048];

    initial {
        $readmemh("tests/test.hex", mem);
    }

    always_comb {
        data = mem[addr];
    }
}
